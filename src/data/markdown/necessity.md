# 猜想和必要性证明

一个数若可由尺规作图得到，则它所在的域扩张次数必须是 2 的幂。

因此正 $n$ 边形可作时，相关的代数扩张次数应满足：

$$
\varphi(n)=2^k \cdot m
$$

在典型的奇素数情形 $n=p$ 下：

$$
\varphi(p)=p-1
$$

若正 $p$ 边形可作，则必须有

$$
p-1=2^k
$$

也就是说

$$
p=2^k+1
$$

进一步可以推出 $k$ 本身必须是 2 的幂，所以 $p$ 必须是费马素数：

$$
p=2^{2^r}+1
$$
